/**
 * Whether to send route request to get route data when backend route fetching is enabled
 *
 * If set to false, it will not send a separate request to get route data, but instead get dynamic route data from the user details interface
 * If set to true, it will send a separate request to get route data
 */
export const isSendRoutingRequest = true;
