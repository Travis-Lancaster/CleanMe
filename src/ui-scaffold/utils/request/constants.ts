// Define the field name in HTTP request headers used for passing authorization information
export const AUTH_HEADER = "Authorization";

// Define the field name in HTTP request headers used for specifying language preferences
export const LANG_HEADER = "X-Lang";

// Define the field name in HTTP request headers used for specifying application name
export const APP_NAME_HEADER = "X-App-Name";
export const REFRESH_TOKEN_PATH = "auth/refresh";
